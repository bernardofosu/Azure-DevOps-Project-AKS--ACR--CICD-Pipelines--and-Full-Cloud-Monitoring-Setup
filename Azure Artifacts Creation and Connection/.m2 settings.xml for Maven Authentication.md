# 🛠️ Editing `~/.m2/settings.xml` for Maven Authentication (with Emojis)

This guide shows you how to **manually create and edit** your Maven `settings.xml` file so Maven can authenticate with **Azure Artifacts Maven Feed**. Perfect for local development, CI/CD, or secure builds. 🚀

---

# 📁 1. Create the `.m2` Directory

Maven stores user-level settings in:

📍 **Linux/macOS:** `~/.m2/`
📍 **Windows:** `C:\Users\<you>\.m2`

Create the folder if it doesn’t exist:

```bash
mkdir -p ~/.m2
```

---

# 📝 2. Open or Create `settings.xml`

You can use any editor.

### ✏️ Linux/macOS:

```bash
nano ~/.m2/settings.xml
```

### 🖥️ Windows (PowerShell):

```powershell
notepad $HOME\.m2\settings.xml
```

---

# 🔐 3. Add Azure Artifacts Authentication Block

Copy the Maven `<server>` block from:
👉 **Azure DevOps → Artifacts → Your Feed → Connect to Feed → Maven**

Example:

```xml
<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0 https://maven.apache.org/xsd/settings-1.0.0.xsd">

  <servers>
    <server>
      <id>AzureDevOps-Feed</id>
      <username>AzureDevOps</username>
      <password>YOUR_PERSONAL_ACCESS_TOKEN</password>
    </server>
  </servers>

</settings>
```

### 🔑 Notes

- `<id>` **must match** the `<id>` in your `pom.xml` repository configuration.
- `password` must be a **PAT token** with:
  **Packaging Read & Write** permissions.

---

# ⚠️ 4. Security Warnings (Important!)

❌ Do **NOT** commit `settings.xml` to GitHub or Azure Repos
❌ Do **NOT** store your PAT in pipelines or scripts
❌ Do **NOT** share this file with anyone

✔️ Always keep it **local and private**
✔️ Use Azure DevOps **MavenAuthenticate@0** task for pipelines

---

# ▶️ 5. Test Maven Authentication

Run:

```bash
mvn deploy
```

If configured correctly, your artifact will upload to Azure Artifacts. 🎉

---

# 🎯 Summary

| Step                   | Description                         |
| ---------------------- | ----------------------------------- |
| 📁 Create `~/.m2`      | Directory for Maven config          |
| 📝 Edit `settings.xml` | Add Azure DevOps server credentials |
| 🔐 Use PAT             | Secure authentication               |
| 🚀 Run `mvn deploy`    | Publish to Azure Artifacts          |

---

If you'd like, I can also create:
✨ Full Maven + Azure Pipeline YAML
✨ Full pom.xml Azure Artifacts template
✨ A Canva-ready visual diagram of Maven → Azure Feed flow

Just tell me! 😊
